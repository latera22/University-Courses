# Node.js dependencies
node_modules/

# Build output
build/
dist/
.parcel-cache/ # If you're using Parcel bundler
.next/        # If you're using Next.js
.vite/        # If you're using Vite

# Environment variables (CRITICAL for your case)
server/.env
.env.local
.env.*.local

# Git LFS cache (usually automatically handled, but good to list)
.git/lfs/

# Large files that should be ignored if not managed by LFS
# (Though LFS is the better solution if you want them versioned)
# server/uploads/courses/*.pdf

# Common OS and editor files
.DS_Store
Thumbs.db
.vscode/
*.log